---
name: create-datasurface-bootstrap
description: Creates a bootstrap DataSurface Yellow system for demos on a local docker desktop with kubernetes environment.
---

# Introduction

The demo is a docker desktop based infrastructure. It requires a working postgres docker container to be running. It requires docker desktop with kubernetes enabled. This repository is the model for this demo system.

This model is in the datasurface/demo-bootstrap repository on GITHUB.
